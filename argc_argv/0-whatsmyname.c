#include <stdio.h>

/**
 * main - prints the program's name as invoked
 * @argc: argument count (unused)
 * @argv: argument vector
 *
 * Return: Always 0.
 */
int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
